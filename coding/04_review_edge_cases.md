# Review edge cases

**Use when:** annotating whether an implementation is robust.

> Inspect this implementation for boundary cases involving empty input, missing fields, repeated values, and unexpected types. Separate definite bugs from reasonable design choices and suggest only actionable changes.
